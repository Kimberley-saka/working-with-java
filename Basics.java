public class Basics {
    public static void main (String[] args) {
       int votingAge = 18;
       //tenary if..else
       String canVote = (votingAge >= 18) ? "Can Vote" : "Cannot Vote";

       System.out.println(canVote);
    }
}
